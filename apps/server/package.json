{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "wrangler dev --port=3002 --config wrangler.local.jsonc",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@morpics/api": "workspace:*",
    "@morpics/auth": "workspace:*",
    "@morpics/buckets": "workspace:*",
    "@morpics/db": "workspace:*",
    "@morpics/sdk": "workspace:*",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@morpics/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "wrangler": "^4.40.3"
  }
}
